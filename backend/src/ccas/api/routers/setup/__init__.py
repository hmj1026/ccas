"""Setup UX 後端 routers（oauth-onboarding-ui change）。

包含：
- ``gmail``: Gmail OAuth Web flow（取代 host CLI）
- ``banks``: 銀行啟用 toggle UI（PR-C3）
- ``secrets``: PDF 密碼 DB 化 UI（PR-C3）
- ``admin``: API token rotate UI（PR-C4）
"""
